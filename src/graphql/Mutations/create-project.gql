mutation CreateProject(
  $name: String!
  $description: String
  $tenantId: String!
) {
  create_project(
    input: { name: $name, description: $description, tenant_id: $tenantId }
  ) {
    id
  }
}
