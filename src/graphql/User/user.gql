query User {
  user {
    id
    username
    email
    settings
    default_membership_id
    first_name
    last_name
    memberships {
      id
      tenant {
        id
        name
        slug
      }
      role_detail {
        id
        name
      }
    }
  }
}
