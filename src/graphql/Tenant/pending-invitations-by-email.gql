query PendingInvitations($email: String!, $tenantIds: [uuid!]) {
  pendingInvitations: membership_invitation(
    where: { _and: [
      { email: { _eq: $email }},
      {tenant_id: {_nin: $tenantIds}} ]}
  ){
    id
    tenant_id
    role
    email
    created
    tenant {
      id
      name
      slug
    }
  }
}
