query CloudHooks($searchParams: [cloud_hook_bool_exp!]) {
  cloud_hook(where: { _or: $searchParams }) {
    id
    active
    config
    created
    name
    states
    tenant_id
    type
    updated
    version_group_id
  }
}
