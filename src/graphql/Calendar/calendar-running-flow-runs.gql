query CalendarRunningFlowRuns($flowIds: [uuid!]) {
  flow_run (
    where: { 
      _and: [
    {flow_id: { _in: $flowIds}},
    {state: { _eq: "Running"}}
      ]
    }
  ) 
  {
    id
    flow_id
    name
    end_time
    start_time
    scheduled_start_time
    state
  }
}