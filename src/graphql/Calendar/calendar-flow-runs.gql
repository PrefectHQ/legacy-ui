query CalendarFlowRuns($project_id: uuid, $startTime: timestamptz) {
  flow_run(
    where: {
    flow: { project_id: { _eq: $project_id } }
    start_time: {_gte: $startTime}
    }
    # limit: 250
  ) {
    id
    flow_id
    name
    end_time
    start_time
    scheduled_start_time
    state
  }
}
