query MembershipInvitation($id: uuid!) {
  membership_invitation_by_pk(id: $id) {
    id
    role_detail {
      id
      name
    }
    email
    created
    tenant {
      id
      name
    }
  }
}
